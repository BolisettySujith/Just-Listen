import { atom } from "recoil";

export const playlistIdState = atom({
    key: "playlistIdState",
    default: "7vLsEMsJ2V4PdllNuvv5aw",
});

export const playlistState = atom({
    key: "playlistState",
    default: null,
});